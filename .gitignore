# Remix compiler artifacts
**/artifacts/
**/artifacts/**

# Remix plugin state folders
deps/
states/

# Debug info
*.dbg.json
*.tsbuildinfo

# Optional
.env
.env.local

# Foundry
cache/
out/

# Keep broadcast/ logs for real deployments (useful for verification later),
# but drop local anvil runs (chain id 31337) and dry-runs.
broadcast/*/31337/
broadcast/*/*/dry-run/
